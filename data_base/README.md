# Base de dados

A estrutura ativa do banco fica nos schemas e migrations Prisma do `server/` e do `controlador/`.

Os artefatos antigos de MySQL foram movidos para `mysql_legacy/` e devem ser tratados apenas como histórico.
