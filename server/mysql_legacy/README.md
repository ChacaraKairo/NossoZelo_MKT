# MySQL legado

Arquivos SQL antigos mantidos apenas como histórico da modelagem anterior.

O banco ativo do projeto é PostgreSQL e a fonte de verdade atual é:

- `server/prisma/schema.prisma`
- `server/prisma/migrations/`

Não use estes arquivos para criar ou migrar ambientes atuais.
